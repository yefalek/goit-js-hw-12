 {{#each data}}
       <span class="country-wrraper"> 
        <img src="{{flag}}" alt="{{name}}" width='50px' height='50px' />
        <h2 class="country-title">{{name}}</h2>
        </span>
    {{/each}}